{
  "name": "triumph-clock-system-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-assets.mjs",
    "check": "tsc --noEmit && vitest run && npm run build && wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "test": "vitest run",
    "types": "wrangler types src/worker-configuration.d.ts"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260814.1",
    "@types/node": "^24.2.1",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.30.0"
  }
}
